{
  "name": "vertex-forge",
  "private": true,
  "version": "0.1.0",
  "description": "Static 3D vertex editor - place individual vertices, build faces, export JSON and GLB.",
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/*.test.js\"",
    "serve": "node tools/serve.cjs"
  }
}
