# Debug probes + their downloads/screenshots (session scratch, not shipped)
scratch/

# Generated by `node tools/make-head-assets.mjs` (~1.6 MB of test binaries;
# the head generator in tools/ is the source of truth and stays committed).
test-assets/

# OS / editor noise
Thumbs.db
Desktop.ini
.DS_Store

# Downloaded third-party audit assets (licensing + size); tools/audit-real-and-head.mjs expects you to fetch them locally
real-assets/

# Node CPU profiles from perf runs
*.cpuprofile
